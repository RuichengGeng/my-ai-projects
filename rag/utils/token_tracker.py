# Canonical location is utils/token_tracker.py (project root).
# This stub keeps existing `from rag.utils.token_tracker import ...` imports working.
from utils.token_tracker import (  # noqa: F401
    UsageTracker,
    _active_tracker,
    get_active_tracker,
    set_active_tracker,
)
